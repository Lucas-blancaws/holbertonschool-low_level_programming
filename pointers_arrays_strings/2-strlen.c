#include "main.h"
/**
* _strlen - parcours les strings
* @s: string
* Description (): parcours string
* Return:valeur 0
*/
int _strlen(char *s)
{
	int n;

	for (n = 0; s[n] != '\0'; n++)
	{
	}
	return (n);
}
